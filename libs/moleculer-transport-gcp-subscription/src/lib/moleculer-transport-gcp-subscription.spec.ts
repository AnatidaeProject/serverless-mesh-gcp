import { moleculerTransportGcpSubscription } from './moleculer-transport-gcp-subscription';

describe('moleculerTransportGcpSubscription', () => {
  it('should work', () => {
    expect(moleculerTransportGcpSubscription()).toEqual(
      'moleculer-transport-gcp-subscription'
    );
  });
});
